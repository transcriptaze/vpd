  <g inkscape:groupmode="layer" inkscape:label="components">    
    {% if inputs -%}
    {% for v in inputs -%}
    <circle cx="{{v.x}}" cy="{{v.y}}" r="{{v.r}}" fill="{{v.colour}}" />
    {%- endfor %}
    {%- endif %}

    {% if outputs -%}
    {% for v in outputs -%}
    <circle cx="{{v.x}}" cy="{{v.y}}" r="{{v.r}}" fill="{{v.colour}}" />
    {%- endfor %}
    {%- endif %}

    {% if parameters -%}
    {% for v in parameters -%}
    <circle cx="{{v.x}}" cy="{{v.y}}" r="{{v.r}}" fill="{{v.colour}}" />
    {%- endfor %}
    {%- endif %}
  </g>
