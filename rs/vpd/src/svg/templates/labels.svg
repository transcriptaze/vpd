  <g inkscape:groupmode="layer" inkscape:label="labels">
    {% if labels -%}
    {% for v in labels -%}
    <g transform="translate({{v.x}},{{v.y}})" fill="{{v.colour}}">
      {{v.path}}
    </g>
    {%- endfor %}
    {%- endif %}
  </g>
